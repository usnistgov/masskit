# RI library, 2023 and later
format: sdf
source: nist
id:
  field: ID
  field_type: int
  initial_value: 0
name:
  field: 
    - NAME
    - _Name
schema_group: mol

# skip computation of computed properties
skip_computed_props: False
# skip computation of computed properties expensive to compute
skip_expensive: True